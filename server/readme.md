###
```
In order to run the go server you will run "go run main.go" from the directory structure parallel to the file.
Middleware and business logic.

Note that you will neeed to appropriately set the GOPATH bash enviroment variable for this work correctly. Follow the medium blog post from Notion in order to do this properly
but effectively you will clone the "Technology" repository into a directory strucure that looks like $GOPATH/src/Technology.

In order to add new backend routes you do so by using the router.
```
